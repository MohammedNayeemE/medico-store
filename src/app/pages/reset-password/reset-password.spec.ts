import { ResetPasswordComponent } from './reset-password';

describe('ResetPasswordComponent', () => {
  it('should create', () => {
    const component = new ResetPasswordComponent({} as any);
    expect(component).toBeTruthy();
  });
});
